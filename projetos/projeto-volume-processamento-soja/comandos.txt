# Comandos que devem ser executados no terminal ou prompt de comando:

# Comandos do Cluster Spark

docker exec spark-master spark-submit --deploy-mode client ./jobs/projeto-volume-processamento-soja.py